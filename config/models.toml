# ---------------------------------------------------------------------------
# Tier -> model chain registry.  ADR-006.
#
# STATUS: DELIBERATELY UNPOPULATED.  OQ-01 is open.
#
# No model ids are written here by guesswork.  A stale or renamed provider id
# fails the way AP-05 describes: silently, with no error and no log line, and
# it invalidates every LangSmith baseline recorded against it.
#
# To populate:   dynaflows models --suggest        (needs OPENROUTER_API_KEY)
#
# That command queries the live model list, keeps only endpoints that support
# `response_format: json_schema`, and prints a TOML block to paste below.
# `dynaflows doctor` then re-validates every id against the live list and
# fails if any is missing or has lost structured-output support.
#
# Bump `version` on every edit.  It is hashed into plan_hash, so a plan is
# never compared across incompatible model sets.
# ---------------------------------------------------------------------------

version = "1"

[constraints]
# ADR-006: a price-ordered chain that degrades to a model without json_schema
# support turns a recoverable rate limit into a wrong answer.
require_structured_outputs = true

# ADR-006: the verifier must not share a model family with the worker it
# checks, or it shares the worker's failure modes and rubber-stamps.
enforce_verifier_family_diversity = true

# ADR-006: context length is a capability. Every model in every chain must
# hold at least this many tokens, or a fallback fails on the exact prompts its
# primary was chosen for.
#
# DERIVED 2026-09-10, from measured values rather than taste. This is a lower
# bound the system provably exceeds, not the true requirement -- tighten it at
# step 1.8 from LangSmith's real per-call token counts (playbook 4.5).
#
#   enhancer  (small)     ~   600  short instruction
#   planner   (frontier)  ~ 5,456  catalogue 3,956 (measured) + prompt + schema
#   worker    (mid)       ~ 3,000  context pack 2,000 + objective + inputs
#   synthesizer (mid_high)~12,008  12 summaries (ADR-014) + 8,000 artifact budget
#   ------------------------------------------------------------------------
#   largest today ~12,008; x2 headroom ~24,016; floor set at 32,000
#
# 0 disables the check. doctor reports the disabled state as WARN, never OK,
# so a skipped check never reads as a passing one.
min_context_tokens = 32000

# Chains are ordered: index 0 is preferred, later entries are fallbacks.
# Every entry in a chain must satisfy the constraints above.

[tiers.small]  # cheapest available; the enhancer and router are easy tasks
chain = [
    "nex-agi/nex-n2.5-mini:free",  # in $0.00 out $0.00 ctx 262k
    "nex-agi/nex-n2.5-pro:free",  # in $0.00 out $0.00 ctx 262k
    "dots-studio/dots-3-note-preview:free",  # in $0.00 out $0.00 ctx 512k
    "liquid/lfm-2.5-2.6b:free",  # in $0.00 out $0.00 ctx 131k
    "nvidia/nemotron-3-super-120b-a12b:free",  # in $0.00 out $0.00 ctx 1000k
]

[tiers.mid]  # the workers. Ordered by MEASURED capability first, price second.
#
# `mistralai/mistral-nemo` was the head of this chain and scored 0/5 on the
# ADR-022 calibration fixture -- it read a file with a logged API key and two
# bare `except` blocks and reported nothing. It is REMOVED, not demoted:
# ADR-006 already forbids a chain that degrades to a model without structured
# outputs, because that turns a rate-limit into a wrong answer. A model that
# cannot do the task does the same thing, more quietly.
#
# Re-measure with `dynaflows calibrate --sweep tiers` after any edit here.
chain = [
    "meta-llama/llama-4-scout",  # MEASURED 4/5, $0.0004/call  ctx 10000k
    "openai/gpt-5-nano",  # UNMEASURED  in $0.02 out $0.20 ctx 400k
    "upstage/solar-pro4",  # UNMEASURED  in $0.03 out $0.12 ctx 524k
    "openai/gpt-oss-20b",  # UNMEASURED  in $0.03 out $0.13 ctx 131k
]

[tiers.mid_high]  # the Phase 2 verifier: family-disjoint from mid per ADR-006
#
# `meta-llama/llama-4-scout` was the head here and moved to `mid` when the
# calibration measured it. It is removed from this chain: the diversity rule
# compares whole chains, and a verifier that can fall back to the same model
# it is checking rubber-stamps exactly when it matters most.
chain = [
    "deepseek/deepseek-v4-flash-0731",  # UNMEASURED  in $0.05 out $0.16 ctx 1048k
    "x-ai/grok-4.20-multi-agent",  # UNMEASURED  in $1.25 out $2.50 ctx 2000k
    "x-ai/grok-4.20",  # UNMEASURED  in $1.25 out $2.50 ctx 2000k
    "google/gemini-3.1-pro-preview-customtools",  # UNMEASURED  in $2.00 out $12.00 ctx 1048k
]

[tiers.frontier]
# NOT SUGGESTED. The planner needs reasoning quality, and nothing in the
# catalogue measures that. Sorting by price would name the most expensive
# model, which is a legacy-premium heuristic, not a good one.
# Uncomment one or two deliberately. ADR-006: this tier runs ONCE per plan
# but decides the cost and correctness of N worker calls.
chain = [
    # "meta-llama/llama-4-scout",  # in $0.10 out $0.30 ctx 10000k
    # "x-ai/grok-4.20-multi-agent",  # in $1.25 out $2.50 ctx 2000k
    # "x-ai/grok-4.20",  # in $1.25 out $2.50 ctx 2000k
    "openai/gpt-5.6-luna-pro",  # in $0.10 out $0.60 ctx 1050k
    # "openai/gpt-5.6-luna-pro:batch",  # in $0.10 out $0.60 ctx 1050k
]