"""The change shape's write half: an isolated checkout, a coding agent, a verdict.

Registered as a package before it has a graph node, because the seam is where
every unknown in Phase 2 lives and each piece is testable on its own: the
worktree against real git, the verdict against a real test run, the agent
against a recorded transcript. Nothing here is imported by the read shape.
"""

from __future__ import annotations
